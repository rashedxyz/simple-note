{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "lerna run dev --parallel",
    "start": "lerna run start --parallel",
    "build": "lerna run build"
  },
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.0.1"
  }
}
